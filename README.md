# Dev UI Demo

This is a .NET 10 app which use the latest .NET DEV UI package.

## How to run

1. Open the repo in codespaces.
2. Open the terminal.
3. Run the command `dotnet run hellodevui.cs`.
4. Modify the forwarded URL and append `/devui` view the Dev UI.